        // if (!message.member.hasPermission('MANAGE_MESSAGES')) { return message.channel.send("Tu n'as pas le droit de faire ça") }
        // const channel = message.guild.channels.cache.find(ch => ch.id === '697463540121862225');
        // const channel = message.guild.channels.cache.find(ch => ch.name.startsWith('labo-des-pjs'));
        // if (!channel) return
